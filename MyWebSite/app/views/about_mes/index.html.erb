<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>About Me</title>
    <link href="https://fonts.googleapis.com/css?family=Bellefair|Fresca|Raleway" rel="stylesheet">
  </head>
  <body>
    <p id="notice"><%= notice %></p>

    <div class="back">
      <%= link_to 'Back to Home', homes_path %>
    </div>

    <h1>This is the page where I describe myself</h1>

    <!--<table>
      <thead>
        <tr>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @about_mes.each do |about_me| %>
          <tr>
            <td><%= link_to 'Show', about_me %></td>
            <td><%= link_to 'Edit', edit_about_me_path(about_me) %></td>
            <td><%= link_to 'Destroy', about_me, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>-->

    <br>

    <!--<%= link_to 'New About Me', new_about_me_path %>-->

  </body>
</html>
