<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Photos</title>
  </head>
  <body>
    <p id="notice"><%= notice %></p>

    <h1>My Photos Page</h1>

    <img src="https://pbs.twimg.com/media/C80DdHAXUAEChRP.jpg:large" alt="Lax Pic" style="width: 100; height: 100">
    <!--<table>
      <thead>
        <tr>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @photos.each do |photo| %>
          <tr>
            <td><%= link_to 'Show', photo %></td>
            <td><%= link_to 'Edit', edit_photo_path(photo) %></td>
            <td><%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>-->

    <br>

    <%= link_to 'New Photo', new_photo_path %>
    <%= link_to 'Back to Home', homes_path %>
  </body>
</html>
